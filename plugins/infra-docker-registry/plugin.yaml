id: infra-docker-registry
name: "Docker Registry"
description: "Local OCI image registry for building and deploying plugin images from within the platform"
group: infrastructure
version: "1.0.0"
image: "registry:2"
author: teamagentica
tags: [infrastructure, docker, registry]
capabilities: ["infra:docker-registry"]
dependencies: []
