id: network-webhook
name: "Webhooks"
description: "Webhook route registry and reverse proxy. Routes external webhook traffic from the ingress to internal plugins."
group: network
version: "1.0.12"
image: "teamagentica-network-webhook:latest"
author: TeamAgentica
tags: [webhook, networking, routing]
capabilities: ["network:webhook"]
config_schema:
  WEBHOOK_PORT:
    type: number
    label: "Listen Port"
    default: 9000
