id: infra-redis
name: "Redis Cache"
description: "Shared Redis cache and event bus for plugin state, event streaming, rate limiting, and ephemeral data"
group: infrastructure
version: "1.0.14"
image: "teamagentica-infra-redis:latest"
author: teamagentica
tags: [infrastructure, cache, redis, memory]
capabilities: ["memory:cache", "infra:events"]
config_schema:
  PLUGIN_DEBUG:
    type: boolean
    label: "Debug Mode"
    default: "false"
    order: 99
