id: infra-agent-memory-gateway
name: "Agent Memory: Gateway"
description: "Unified memory gateway — routes requests to semantic (Mem0) and episodic (LCM) memory backends"
group: infrastructure
version: "0.4.4"
image: "teamagentica-infra-agent-memory-gateway:latest"
author: teamagentica
tags: [memory, gateway, agents]
capabilities: ["tool:memory"]
config_schema:
  PLUGIN_DEBUG:
    type: boolean
    label: "Debug Mode"
    description: "Enable debug logging"
    default: "false"
    required: false
    order: 99
